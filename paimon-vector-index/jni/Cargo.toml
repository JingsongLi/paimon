[package]
name = "paimon-vindex-jni"
version = "0.1.0"
edition = "2021"

[lib]
name = "paimon_vindex_jni"
crate-type = ["cdylib"]

[dependencies]
paimon-vindex-core = { path = "../core" }
jni = "0.21"
